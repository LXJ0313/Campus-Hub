1. 搜索入口
首页
首页保留搜索框和分类入口。
用户在首页点击搜索框或分类后，跳转至 /search 搜索页。
首页不直接执行搜索或分类筛选。
搜索页

搜索页是 V1 唯一的搜索与分类检索页面。

2. 搜索

搜索页仅保留一个 **「AI 搜索」**按钮。

用户可以输入：

简单关键词，例如：篮球、AI、产品经理
自然语言，例如：适合研究生参加的 AI 活动

两种输入均通过 AI Search 处理。

按 Enter 与点击「AI 搜索」执行相同操作。

3. 分类

搜索页提供分类筛选。

分类可以独立使用：

用户未输入搜索词 → 选择分类 → 显示该分类下的活动。

例如：

Sports → 显示 Sports 活动。

分类使用精确匹配，不进行模糊匹配。

4. 搜索 + 分类

搜索词与分类同时存在时，必须使用 AND 逻辑：

搜索结果 = 满足搜索条件 且 属于所选分类的活动。

例如：

AI + Lecture

只显示同时满足：

与 AI 搜索条件匹配
Category = Lecture

的活动。

5. AI Search 的关键词匹配

对于包含多个明确条件的查询，应尽可能保持条件之间的 AND 关系。

例如：

「适合研究生参加的 AI 活动」

应理解为：

AI + 研究生

而不是：

AI 或 研究生。

只有同时满足主要搜索条件的活动才应进入结果。

6. 无结果

如果没有符合条件的活动：

显示「没有找到相关活动，试试其他关键词」。

不得自动展示 Demo Activity 或其他无关活动作为兜底结果。

7. V1 不做的事情
不保留普通「搜索」按钮。
不在首页直接执行搜索。
不使用 Demo Activity 作为搜索失败时的 fallback。
不因为某一个关键词匹配就放宽其他搜索条件。
不改变现有数据库 Schema。
最核心的规则

首页负责进入搜索页；搜索页负责搜索和分类。

搜索条件之间默认取 AND；分类与搜索条件同时存在时也取 AND；无结果就显示空状态，不兜底。